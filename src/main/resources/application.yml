spring:
  application:
    name: Hcom
  profiles:
    active: local

server:
  port: 8081
  context-path: /hcom

---

spring:
  profiles: local

database:
  mongo:
    database: hcom
    host: localhost

logging:
  level:
    org:
      schlocknet: DEBUG
